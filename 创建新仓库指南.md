# 创建 GitHub 新仓库指南 - elite20-starter-LH

## 目标
将 `elite20-starter-LH` 项目创建为一个独立的 GitHub 仓库，而不是任何人的分支。

---

## 步骤一：在 GitHub 上创建新仓库

1. 访问 https://github.com/new
2. **Repository name**（仓库名称）：`elite20-starter-LH`
3. **Description**（描述）：Elite20 入门项目 - AI 编程助手
4. 选择 **Public**（公开）或 **Private**（私有）
5. **不要**勾选 "Initialize this repository with a README"
6. **不要**添加 .gitignore
7. **不要**选择 License
8. 点击 **Create repository** 按钮

---

## 步骤二：提取项目内容并创建新仓库

在你的电脑上执行以下操作：

### 方案 A：手动创建新仓库（推荐）

```powershell
# 1. 进入 GitHub 目录
cd d:\onenotes\OneDrive\文档\GitHub

# 2. 创建新的仓库目录
mkdir elite20-starter-LH-new
cd elite20-starter-LH-new

# 3. 初始化 git
git init

# 4. 从原仓库复制你的项目内容
# 手动复制以下内容：
# - students/elite20-starter-LH/ 目录下的所有文件
# 复制到 elite20-starter-LH-new/ 目录

# 5. 添加文件并提交
git add .
git commit -m "Initial commit: AI 编程助手 - Elite20 入门项目"

# 6. 添加远程仓库（将 YOUR_USERNAME 替换为你的 GitHub 用户名）
git remote add origin https://github.com/lihuaok/elite20-starter-LH.git

# 7. 推送到 GitHub
git branch -M main
git push -u origin main
```

### 方案 B：使用 git subtree（更高级）

```powershell
# 1. 进入原仓库目录
cd d:\onenotes\OneDrive\文档\GitHub\elite20-starter

# 2. 使用 subtree 拆分项目
git subtree split --prefix=students/elite20-starter-LH -b split-branch

# 3. 创建新目录并克隆拆分后的分支
cd ..
mkdir elite20-starter-LH
cd elite20-starter-LH
git init
git pull ../elite20-starter split-branch

# 4. 推送到 GitHub
git remote add origin https://github.com/lihuaok/elite20-starter-LH.git
git branch -M main
git push -u origin main
```

---

## 步骤三：具体操作说明（详细版）

### 第 1 步：准备新目录

1. 在文件资源管理器中打开 `d:\onenotes\OneDrive\文档\GitHub\`
2. 创建一个新文件夹，命名为 `elite20-starter-LH`

### 第 2 步：复制文件

1. 打开 `d:\onenotes\OneDrive\文档\GitHub\elite20-starter\students\elite20-starter-LH\`
2. 复制以下所有内容到新文件夹：
   - `README.md`
   - `REUSE.md`
   - `prompts/` 目录
   - `reflections/` 目录
   - `artifacts/` 目录
   - `coordinate-cards/` 目录
   - `kstar/` 目录

### 第 3 步：在新目录中初始化 git

打开 PowerShell，执行：

```powershell
cd d:\onenotes\OneDrive\文档\GitHub\elite20-starter-LH
git init
git add .
git commit -m "Initial commit: AI 编程助手 - Elite20 入门项目"
```

### 第 4 步：关联 GitHub 仓库

```powershell
# 关联远程仓库
git remote add origin https://github.com/lihuaok/elite20-starter-LH.git

# 重命名分支为 main
git branch -M main

# 推送到 GitHub
git push -u origin main
```

---

## 步骤四：验证新仓库

1. 访问 https://github.com/lihuaok/elite20-starter-LH
2. 确认你的项目文件都已正确上传
3. 检查 README 是否正确显示
4. 确认这是一个独立的仓库，不是任何人的分支

---

## 步骤五：（可选）更新原仓库

如果你想从原仓库中删除该项目（可选）：

```powershell
cd d:\onenotes\OneDrive\文档\GitHub\elite20-starter
git rm -r students/elite20-starter-LH
git rm ai_logs/chat_log_elite20-starter-LH.md
git commit -m "Remove elite20-starter-LH (moved to separate repo)"
git push
```

---

## 重要提醒

✅ **确保**：新仓库名称是 `elite20-starter-LH`  
✅ **确保**：使用你的 GitHub 账号 `lihuaok`  
✅ **确保**：这是一个独立的仓库，不是 fork  
✅ **记得**：你的座右铭 - 完成任务是能力答卷，超越任务是认知革命

---

## 遇到问题？

如果遇到权限问题或需要帮助，请：
1. 确认你已登录 GitHub 账号
2. 检查 git 配置：`git config user.name` 和 `git config user.email`
3. 如果需要，使用 Personal Access Token 进行身份验证

---

**指南创建时间**：2026-05-01  
**项目主题**：AI 编程助手
